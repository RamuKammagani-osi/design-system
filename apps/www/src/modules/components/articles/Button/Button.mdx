import {
  PrimalButton,
  Button,
  Icon,
  IconButton,
  Card,
  CardBody
} from "@cwds/components";
import { DemoCard, PropTable, ReferenceCard } from "@cwds/docs";
import Docgen from "../ReactDogenInfo.js";

## Synopsis

`Button` should be use in most CARES Application unless a button is a different theme or customize

<DemoCard>
  <div>
    <Button primary>Click Me</Button> <Button>Go Back</Button>
  </div>
</DemoCard>

```jsx
import { Button } from "@cwds/components";
```

`Button`s indicate an opportunity for users to trigger a system response when activated.

## Usage Guidelines

The `Button` is the dominant component for indicating Call To Action (CTA) opportunties to the user. Within a particular context, there may exist multiple CTAs and

### Primary Buttons

- Primary buttons should be used to call attention to an action on a form, or highlight the strongest calls to action on a page, or in a card.
- Keep in mind that not every screen needs a primary button.

### Secondary Buttons

- Secondary buttons should be used for secondary actions, semi-destructive actions such as "Cancel," or to discourage usage.

### Buttons with Icon

- Buttons with Icon may be used when you want to draw more attention to the button, or when an icon helps to clarify or convey more meaning.

<div className="d-flex justify-content-around m-3">
  <IconButton icon="circle-notch" spin color="primary" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="secondary" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="accent" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="success" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="info" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="warning" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="danger" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="light" size="sm">
    Busy
  </IconButton>
  <br />
  <IconButton icon="circle-notch" spin color="dark" size="sm">
    Busy
  </IconButton>
</div>

## Props

<PropTable docgen={Docgen.Button} />

## Examples

### Button

<div className="my-3">
  <Button primary>Primary</Button> <Button>Default</Button>
</div>

```jsx
<Button primary>Primary</Button>
<Button>Default</Button>
```

### PrimalButtons

<ReferenceCard>
  The <code>Button</code> component exported from <code>@cwds/components</code>{" "}
  is an abstraction on top of the <code>Button</code> from{" "}
  <code>reactstrap</code>. We've done this promote consistency of button usage
  in CWDS service modules. If you <em>must</em> drop down into{" "}
  <code>reactstrap</code>'s <code>Button</code>, it is exported as{" "}
  <code>PrimalButton</code>.
</ReferenceCard>

Buttons take a color prop which should be one of the thematic color values:

<div className="my-3">
  <PrimalButton color="primary">primary</PrimalButton>{" "}
  <PrimalButton color="secondary">secondary</PrimalButton>
</div>

```jsx
<PrimalButton color="primary">primary</PrimalButton>
<PrimalButton>secondary</PrimalButton>
```

### Contextual Colors

<div className="my-3">
  <PrimalButton color="success">success</PrimalButton>{" "}
  <PrimalButton color="info">info</PrimalButton>{" "}
  <PrimalButton color="warning">warning</PrimalButton>{" "}
  <PrimalButton color="danger">danger</PrimalButton>
</div>

```jsx
<PrimalButton color="success">success</PrimalButton>
<PrimalButton color="info">info</PrimalButton>
<PrimalButton color="warning">warning</PrimalButton>
<PrimalButton color="danger">danger</PrimalButton>
```

### Light/Dark

<div className="my-3">
  <PrimalButton color="light">light</PrimalButton>{" "}
  <PrimalButton color="dark">dark</PrimalButton>
</div>

```jsx
<PrimalButton color="light">light</PrimalButton
<PrimalButton color="dark">dark</PrimalButton>
```

### Links

<div className="my-3">
  <PrimalButton color="link" href="#links">
    link
  </PrimalButton>
</div>

```jsx
<PrimalButton color="link" href="#links">
  link
</PrimalButton>
```

and

<div className="my-3">
  <PrimalButton color="light">light</PrimalButton>{" "}
  <PrimalButton color="dark">dark</PrimalButton>{" "}
  <PrimalButton color="link" href="/">
    link
  </PrimalButton>
</div>

```jsx
<PrimalButton color="light">light</PrimalButton>{' '}
<PrimalButton color="dark">dark</PrimalButton>{' '}
<PrimalButton color="link" href="/">link</PrimalButton>
```

<!--
### Patterns

@TODO

### Components

@TODO -->

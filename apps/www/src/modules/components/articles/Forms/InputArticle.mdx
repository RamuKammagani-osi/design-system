import { Input } from "@cwds/forms";
import { DemoCard, PropTable } from "@cwds/docs";

## Synopsis

Use an `Input` in to capture uers input in forms

<DemoCard>
  <Input placeholder="I am simple Input" />
</DemoCard>

```jsx
import { Input } from "@cwds/forms";
```

## Props

TODO

## Usage Guidelines

TODO

## Examples

### Types

with `type="tel"`

<Input type="tel" />

<Input type="password" />

<Input type="number" />

### FormControl States

with `valid`:

<Input valid defaultValue="Melhouse" />

with `invalid`:

<Input invalid defaultValue="Oh No's" />

with `disabled`:

<Input disabled defaultValue="I am disabled" />

with `readOnly`:

<Input readOnly value="I am readonly" />

## Related Articles

laksdjf

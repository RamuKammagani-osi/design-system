import { DemoCard, CodeBlock } from '@cwds/docs'
import {
  Alert,
  CardBody,
  CardHeader,
  CardTitle,
  Form,
  FormGroup,
  Input,
  Label,
  UncontrolledCollapsibleCard as CollapsibleCard,
} from '@cwds/components'
import RadioGroupExample from './examples/RadioGroupExample'
import RadioGroupValidationExample from './examples/RadioGroupValidationExample'

## Synopsis

Use a `RadioGroup` in forms.

<DemoCard>
  <RadioGroupExample
    label="Metasyntactic Variables"
    options={[
      { value: 'foo', label: 'Foo' },
      { value: 'bar', label: 'Bar' },
      { value: 'bizz', label: 'Bizz (disabled)', disabled: true },
      { value: 'bazz', label: 'Bazz' },
    ]}
    initialValues={'bar'}
    noDebug
  />
</DemoCard>

```jsx
import { RadioGroup } from '@cwds/components'
```

## Props

<CollapsibleCard initialClosed>
  <CardHeader>
    <CardTitle>
      <code>RadioGroup</code> Props
    </CardTitle>
  </CardHeader>
  <CardBody className="pb-0">
    <Alert color="warning">
      We're working on a way to generate <code>PropTables</code> from
      our TypeScript components. Below is manual copy of the prop interface
      for your convenience, but view the <a href="">source on GitHub</a>{' '}
      if you experience any issues.
    </Alert>

```ts
interface RadioGroupProps<T> {
  /** The currently selected value */
  value: T
  /** Possible choices */
  options: Array<{
    /** Visual label */
    label: string
    /** Value associated with the option */
    value: T
    /** Whether or not to enable the option */
    disabled?: boolean
  }>
  /** Name for the field. Used to generate unique id */
  name: string
  /** Whether or not to enable the _entire_ field */
  disabled: boolean
  /** Use alternate layout */
  inline: boolean
  /** Change handler */
  onChange: (values: T[]) => void
  /** Blur handler */
  onBlur: () => void
  /** Legend */
  legend: string
}
```

  </CardBody>
</CollapsibleCard>

## Usage Guidelines

## Examples

### In a FormField

<RadioGroupValidationExample />

## Related Articles

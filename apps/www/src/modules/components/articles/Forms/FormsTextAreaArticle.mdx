import { Link } from 'react-router-dom'
import { DemoCard, PropTable, CodeBlock } from '@cwds/docs'

## Synopsis

An HTML `textarea` is rendered when using an `Input` component with `type="textarea"`.

Please see the <Link to="/components/forms/input#textareas">Textarea section in Input Article</Link>.
